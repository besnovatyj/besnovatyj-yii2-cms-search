<?php

/*
 * Copyright (c) 2026 Besnovatyj. Licensed under the MIT License.
 */

declare(strict_types=1);

/**
 * Требования фасада поиска, проверяемые менеджером модулей при установке.
 *
 * Модулей в зависимостях нет: фасад работает и в одиночку — он собирает каталог документов и
 * показывает страницу состояния. Без ядра поиск лишь не находит ничего и честно об этом сообщает,
 * а ядро, наоборот, требует фасад — зависимость односторонняя и объявлена на его стороне.
 *
 * `mbstring` — обязателен: длины запросов, обрезка фрагментов и нормализация текста считаются
 * в символах, а не в байтах.
 */
return [
    'modules' => [],
    'php_extensions' => ['mbstring'],
    'php_version' => '>=8.4',
];
